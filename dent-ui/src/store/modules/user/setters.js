let setters = {};

// setters.user = state => {
//   return state;
// };
//
// setters.username = (state, username) => {
//   state.username = username;
//   return state.username;
// };

export default setters;
